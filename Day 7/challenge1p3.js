/*
........................Coding Challenge #1...............................

//........Data 1............
let arr = [17, 21, 23];

function printForecast(arr) {
  forecast = "";
  for (let i = 0; i < arr.length; i++) {
    forecast = forecast += `${arr[i]}ºC in day ${i + 1} ... `;
  }
  console.log("...." + forecast);
}

printForecast(arr);




//........Data 2............
let arr = [12, 5, -5, 0, 4];

function printForecast(arr) {
  forecast = "";
  for (let i = 0; i < arr.length; i++) {
    forecast = forecast += `${arr[i]}ºC in day ${i + 1} ... `;
  }
  console.log("...." + forecast);
}

printForecast(arr);

*/
